name: Static Site CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Verify static files exist
        run: |
          echo "Checking frontend/public contents"
          ls -la frontend/public
